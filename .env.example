# KAUSHAL-SETU Environment Configuration
# Copy this file to .env and update with your production values

# JWT Authentication Secret (REQUIRED - generate with: openssl rand -hex 32)
SECRET_KEY=kaushal-setu-dev-key-change-in-production-abc123

# JWT Token Expiration (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Encryption Key for Mail (REQUIRED - generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
MAIL_ENCRYPTION_KEY=

# Database URL (optional, defaults to SQLite)
# DATABASE_URL=postgresql://user:password@localhost:5432/kaushal_setu

# Force HTTPS in production (set to "true" for production)
FORCE_HTTPS=false

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# AI Service API Key (optional)
GEMINI_API_KEY=
